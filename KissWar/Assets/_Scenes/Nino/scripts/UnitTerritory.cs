using UnityEngine;
using System.Collections;

public class UnitTerritory : MonoBehaviour {

   /* public BoxCollider2D territory;
    GameObject enemy;
    GameObject unit;

    bool enemyInTerritory;

    FightControl fightControl;

    // Use this for initialization
    void Start () {
        enemy = GameObject.FindGameObjectWithTag("Enemy");
        unit = GameObject.FindGameObjectWithTag("Unit");
        fightControl = fightControl.GetComponent<FightControl>();
        enemyInTerritory = false;
	}
	
	// Update is called once per frame
	void Update () {
        if(enemyInTerritory == true)
        {
            fightControl.MoveUnit();
        }

      
	
	}

    void OnTriggerEnter(Collider other)
    {
        if(other.CompareTag("Enemy"))
        {
            enemyInTerritory = true;
        }
    }

    void OnTriggerExit(Collider other)
    {
        if (other.CompareTag("Enemy"))
        {
            enemyInTerritory = false;
        }
    }
    */
}
